# 更新记录

本文件记录每个版本改了什么，方便回溯和接着迭代。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

---

## [1.3] — 2026-09-18

这一版只动外观：**App 图标换成 ￥**，配色定为「金 → 橙」渐变。
功能一行没改 —— 数据结构、备份格式、界面行为与 v1.2 完全一致。

### 变更

- **图标图形换成 ￥**。原来是「白卡片 + 三条横线 + 一个圆点」的账本图案，
  现在是一枚白色 ￥ 压在品牌渐变底上。
  - **￥ 用描边路径画，不用 `<text>`**：`public/icon.svg` 会被当 favicon 和
    manifest 里的图标直接用，若用文字，设备上缺对应字体就会渲染成一个方框。
    字形（1024 视口，`stroke-width: 80`，圆头）：
    左撇 `M280 232 L512 462`、右捺 `M744 232 L512 462`、竖 `M512 462 L512 792`、
    两横 `M336 568 L688 568` 与 `M336 692 L688 692`。
  - **配色改成金 → 橙渐变**（`#C79A2E` → `#D9662A`，135°）。
    三个 SVG 各自写了一份同样的渐变，`make-icons.mjs` 里另有 `BRAND_FROM` / `BRAND_TO`
    —— **改配色时这几处要一起改**，否则图标和启动画面会不同色。
- **安卓自适应图标的背景层：从「纯色资源」改成「渐变图片」**。
  这是这一版唯一会让人白折腾的坑：`mipmap-anydpi-v26/ic_launcher.xml` 原来写的是
  `<background android:drawable="@color/ic_launcher_background"/>`，一个纯色。
  **改渐变就不能再用 `@color`**（安卓的 color 资源不支持渐变），
  改成 `@mipmap/ic_launcher_background`，由 `make-icons.mjs` 生成五档密度的渐变 PNG。
  漏改这一处的话，安卓 8.0+ 桌面上会是「新前景的 ￥ 浮在旧蓝底上」。
  `values/ic_launcher_background.xml` 留作兜底色，已同步成 `#C79A2E`。
- **自适应图标的前景层要把 ￥ 缩到 0.67 倍**。
  安卓只显示那层 108dp 中间 66% 那块、并放大到图标大小；
  若按整块图标那样铺（字形占本层 62.5%），裁完等于放大到 94%，两撇会顶到图标边缘。
  反推：要让裁完仍是 62.5%，就得乘 0.67（62.5 ÷ 66.7 ≈ 0.94 明显偏大）。
  同理 maskable 用 0.9（最坏会被裁成直径 80% 的圆，要留边），
  iOS 与 apple-touch-icon 用 1.0。
- **安卓启动画面**：原来是蓝底 + 白色卡片，现在是金橙渐变底 + 居中白色 ￥。
  符号尺寸从屏幕短边的 34% 调到 48%（￥ 的墨迹只占自己那个方框的 62%，
  乘下来约是短边的 30%）。
- **版本号 1.2 → 1.3**，五处一起改：`package.json` / `android/app/build.gradle`
  （`versionCode 3→4`）/ `ios/.../project.pbxproj`（`MARKETING_VERSION`、
  `CURRENT_PROJECT_VERSION` 3→4）/ `public/sw.js` 的 `CACHE` / `utils.js` 的 `APP_VERSION`。

### 没有动的

- **App 界面配色仍然是蓝色**（`styles.css` 的 `--primary: #3b7dd8`）。
  图标是金橙、界面是蓝，是刻意分开的：换图标不该顺手把整个界面的调性推翻。
  想统一的话改 `--primary` 一处即可，另外要同步 `manifest.webmanifest` 与
  `index.html` 里的 `theme_color`（还有 `src/main.js` 里切深色时改它的那行）。
- 功能、数据结构、备份格式与 v1.2 完全一致 ——
  **v1.2 → v1.3 可以直接覆盖安装**（同一把签名密钥），数据不动。

### 测试

`tools/verify.mjs` 的 68 项断言全部通过（这次没动功能，也没加断言）。
图标本身不是断言能覆盖的东西，所以新增了一个专门的工具 `tools/verify-icons.mjs`
（跑 `npm run icons:check`）：把生成好的图标按各平台实际看到的样子拼成
`tools/shots/icon-check.png` —— 包括模拟安卓启动器对自适应图标的裁切
（只取 108dp 图层中间 66% 再放大到图标大小）、maskable 的圆形裁切、
iOS 的 1024 源图，以及缩小显示的启动图。
**第一版就是这么被抓出来的** —— 前景层没缩，模拟裁切后 ￥ 顶到了边缘，
而源码和 1024 预览图看起来都很正常。

---

## [1.2] — 2026-09-18

这一版只做一件事：**分类预算** —— v1.1 里列为「下一轮候选 #1」的那条。
仍然是纯本地、零新依赖、零网络请求。

### 新增

- **分类预算：给单个支出分类设月度上限**
  - **数据**：`category` 上新增**可选**字段 `budgetFen`（整数，单位分）。
    「不设预算」归一成**没有这个属性**，不是 0 —— 原因见下面单开的一节。
  - **设置**：分类管理里点任一分类 → 多一格「月预算」。**只有支出分类有**：
    把「类型」改成收入，那一格会自动收起，保存时预算一并清掉。
  - **列表**：分类管理每行的小字从「N 笔记录在用」变成
    「N 笔记录在用 · 本月 138.40 / 100.00 元（138%）」，超过 80% 变黄、超支变红。
  - **记账页分类格右上角加一个小圆点**：灰＝正常，黄＝花到八成，红＝本月已超支；
    没设预算的分类一个圆点都不显示。分类格下面补一行图例。
  - 圆点看的是**所选日期所在的那个月**，不是「今天这个月」——
    补记上个月的账时，看到的该是上个月花掉多少。
  - 圆点本身只是个纯色块，屏幕阅读器读不出任何信息，所以每个带预算的分类格
    都补了 `title` + `aria-label`，例如
    「餐饮：本月已花 38.50 元，预算 100.00 元，还剩 61.50 元（已用 39%）」。
- **`categoryTotals(month, kind, ledgerId)`** —— 抽出来的「按分类汇总」。
  统计页饼图的 `categoryBreakdown` 和新的 `categoryBudgetMap` 现在共用它。
- **`categoryBudgetMap(month, ledgerId)`** —— 一次算完所有设了预算的分类在本月的状态：
  `{ budgetFen, spentFen, leftFen, ratio, level }`，`level` 取 `ok | warn | over`。
  记账页有 9 个支出分类，逐个查要扫 9 遍全部记录，所以先汇总一轮再查表。

### 修复

- **设置页「关于」里的版本号一直写死是 v1.0** —— v1.1 那一版漏改了。
  现在版本号只有一个来源：`utils.js` 的 `APP_VERSION`，设置页只读它。
- **`[hidden]` 在 `.field` 上不生效** —— `[hidden]` 的 UA 样式是 `display:none`，
  但作者样式里任何一条 `display` 都能盖掉它，而 `.field` 正好是 `display:flex`。
  分类编辑面板里那格「月预算」就是靠 `hidden` 藏的，少了兜底会永远藏不住。
  已在 `styles.css` 顶部加 `[hidden] { display: none !important; }` 全局钉死。

### 变更

- **版本号 1.1 → 1.2**，五处一起改：`package.json` / `android/app/build.gradle`
  （`versionCode 2→3`）/ `ios/.../project.pbxproj`（`MARKETING_VERSION`、
  `CURRENT_PROJECT_VERSION` 2→3）/ `public/sw.js` / `utils.js` 的 `APP_VERSION`。
- **`public/sw.js` 的 `CACHE` 升到 `jizhang-v1.2`** —— 这个必须跟着升，
  否则 Service Worker 的 `activate` 不会清掉旧缓存，已「添加到主屏幕」的用户拿不到新资源。

### 测试

`tools/verify.mjs` 的断言从 **53 项加到 68 项**，新增的全是分类预算：
三种圆点状态（灰 / 黄 / 红）、没设预算就没有圆点、图例出现、
换成没记录的月份退回正常、切回今天又变超支、备份里带上 `budgetFen`、恢复后预算还在。
截图 20 → 22 张。**68/68 通过，0 个 JS 报错。**

写测试时踩到一个坑，记在这儿：**点「今天 / 昨天 / 前天」不能用 `page.click()`**。
记账页一旦出现「本月预算」卡片，内容就比一屏长，那三个 chip 会落到固定底栏底下，
按坐标点会**打到底栏的「账单」标签上**，症状是下一句报「找不到 `.cat-grid`」。
改用 JS 触发（`clickEl()`）就好了。

### 兼容性

- **数据库结构未变**，`DB_VERSION` 没升，老数据直接可用。
- **备份格式未变**。`budgetFen` 是分类上的纯新增可选字段，双向都能读：
  - v1.0 / v1.1 导出的旧备份，在 v1.2 里恢复 → 所有分类都没有预算，行为跟以前完全一样。
  - v1.2 导出的备份，在 v1.1 里恢复 → 分类上多一个 `budgetFen`，v1.1 既不读也不写，
    等于忽略，**不会报错**。
- **CSV 导出未变**，不含预算信息（预算不是流水）。

### 为什么「不设预算」归一成「属性不存在」，而不是像账本那样用 0

`ledgers.budgetFen` 一直是「0 表示不设」，最初也想在 category 上照抄。问题在于
分类的预算输入框只在「支出」时显示：

- 用户给「餐饮」设了 1000 的预算，之后把「餐饮」改成收入分类 → 输入框收起，
  那 1000 就**再也看不见、也改不掉**，却会跟着备份一路传下去。
- 反过来，收入分类上挂一个「支出预算」，本身就是没有意义的数据。

所以 `updateCategory()` 里统一归一：`kind === 'income'` 或 `budgetFen <= 0` 时
直接 `delete cat.budgetFen`。读到旧数据（属性不存在）和读到 0，行为完全一致。

---

## [1.1] — 2026-09-18

这一版做了两件事：**修掉 v1.0 的排版与交互问题**（共 20 条，逐条见 `记账本-优化建议.md`），
**并让 iPhone 也能装**。全程没有引入新框架、新依赖、新网络请求，仍然是纯本地 App。

### 新增

- **iPhone 支持**
  - PWA：新增 `public/manifest.webmanifest` + `public/sw.js`，Safari「添加到主屏幕」后全屏运行、支持离线。
  - 原生 iOS：新增 `ios/` 工程（Capacitor 6），图标与启动画面按苹果规范生成。
  - `index.html` 补 `apple-touch-icon`、`apple-mobile-web-app-*` 等 iOS 专用 meta。
- **账单页「本月 / 全部时间」切换** —— 以前 `monthTransactions()` 先按月过滤再搜关键字，
  想找「三个月前买空调那笔」得逐月翻；现在切到「全部时间」一次搜完。
- **删除可撤销** —— 删完弹一条带「撤销」的提示，6 秒内点了就恢复（`restoreTransaction`）。
- **统计页环比** —— 单月总结里多一行「比上月多花 412.30 元（+18%）」，涨红跌绿。
- **分类排序** —— 分类管理每行右侧加上移 / 下移按钮（`moveCategory`），常用的能调到顺手的位置。
- **备份提醒** —— 超过 30 天没导出过备份，记账页顶部出现提示条；导出后记下 `meta.lastBackupAt`，
  设置页显示「上次备份：X 天前」。
- **记账页快捷日期** —— 「今天 / 昨天 / 前天」三个 chip，补记不用翻日历。
- **金额输入实时纠正** —— 新增 `sanitizeAmount()`，输入 `1.2.3` 会被截成 `1.2`（只留一个小数点、最多两位）。

### 修复

- **顶栏标题不居中** —— `#topbar` 原来是 `flex`，左侧账本按钮宽 92px、右侧图标 40px，
  `h1{flex:1}` 只在剩余空间里居中，实测偏右约 26px。改用
  `grid-template-columns: minmax(0,1fr) auto minmax(0,1fr)` 后回到屏幕正中。
- **两张图表的字号一个被放大、一个被缩小** —— `.chart-wrap svg{width:100%}` 覆盖了 svg 固有尺寸，
  文字按 `viewBox` 比例一起缩放：趋势图 viewBox 340 在 360px 屏上缩到 0.89×（10px 字变 ≈9px），
  饼图 viewBox 200 反而放大到 1.52×（12px 字变 ≈18px）。现统一 `VIEW_W = 320` 并给
  `.chart-wrap` 设 `max-width:360px`，缩放比稳定在 1 附近。
- **趋势图图例被 `flex:1` 撑开** —— 「收入 / 支出」四个元素挤在同一个 `.legend-row` 里，
  第一个 `.legend-name` 吃掉剩余空间把「支出」推到最右。拆成两个 `.legend-group` 后并排显示。
- **轻提示挡住了「保存这笔」按钮** —— `.toast` 浮在底部导航之上且 `pointer-events:auto`，
  点保存时其实点到的是 toast，导致第 2、3 笔交易**静默保存失败**（走查里表现为
  「收入合计 期望 8000 实际 128000」）。改成 `.toast{pointer-events:none}` +
  `.toast-action{pointer-events:auto}`，只让撤销按钮接收点击。
- **新建分类的 `order` 会撞号** —— 原来用 `sameKind.length` 算序号，删过分类之后再新建就会重复。
  改成 `max(order) + 1`。
- **`tools/build-apk.mjs` 在 Windows 上必然失败** —— 原来用
  `cmd.exe /d /s /c "\"路径\gradlew.bat\" assembleRelease"`，Node 会把内嵌引号转义成 `\"`，
  而 cmd 不认识反斜杠转义，于是把 `\"C:\...\gradlew.bat\"` 整个当成命令名去找，报
  「不是内部或外部命令」。现在改成 `spawnSync(gradlew, ['assembleRelease'], { shell: true })`。
- **iOS 状态栏文字在浅色主题下看不见** —— `apple-mobile-web-app-status-bar-style` 原来用
  `black-translucent`，该模式下 iOS 状态栏文字**恒为白色**且页面顶到状态栏下面，
  而浅色主题顶栏是 `#ffffff`，白字压白底。改成 `default`（系统按当前外观自动取色）。
- **`Info.plist` 的设备能力声明过时** —— `UIRequiredDeviceCapabilities` 从模板默认的 `armv7`
  改为 `arm64`（Capacitor 6 只支持 iOS 13+，全是 64 位设备；留着 armv7 会导致 App Store 校验报错）。
- **`fileio.js` 分享失败没提示** —— `Share.share` 包了 try/catch，返回
  `{mode:'share-failed', uri, message}`，不再静默失败。

### 变更

- **底部导航图标从 emoji 换成内联 SVG** —— `✏️📋📊⚙️` 在各品牌 ROM 上渲染不一致，
  且无法跟随主题变色（选中的 tab 只有文字变蓝、图标还是原色）。现在用
  `stroke="currentColor"`，颜色自动跟主题走。
- **间距统一** —— 原来 `.card` 12px / `.day-group` 14px / `.set-group` 18px 三套混用，
  改成 `--gap-1..4`（8/12/16/24）一套刻度。
- **次级字号 11px → 12px** —— 11px 在手机上已接近可读下限。
- **统计页大数字加 `tabular-nums`** —— 翻月份时三列不再轻微抖动。
- **分类网格改自适应** —— 固定 5 列时每格约 63px，6 个中文字会被 `ellipsis` 截断；
  改成 `repeat(auto-fill, minmax(64px, 1fr))`。
- **默认分类图标 🧧 → 💵** —— 🧧 是 Unicode 9.0，在安卓 5.1 的老字体上是豆腐块；💵 是 6.0，安全。
  （🛒 同样是 9.0，但它已是「购物」的默认图标、改动影响面大，暂时保留。）
- **表单控件统一 16px + `appearance:none`** —— 防 iOS 聚焦时自动放大，并去掉各平台原生外观。
- **切 Tab 记住滚动位置** —— 新增 `scrollMemory`，不再每次 `scrollTo(0,0)`。
- **版本号 1.0 → 1.1** —— 见下方「版本号在哪几处」。

### 兼容性

- **数据库结构未变**，`DB_VERSION` 没有升，老数据直接可用。
- **备份文件格式未变**，v1.0 导出的备份在 v1.1 里能正常恢复。
- **⚠️ 签名密钥换了** —— 见下方「关于签名」。
- 原来的 `ledger_date` 索引建了但从没被查询用过。**这次故意没删**：删索引要升 IndexedDB
  版本号，会牵动老用户的数据，代价不划算，已在 `db.js` 加注释说明。

### 没做的

原清单里有三条这次**没动**，原因如下：

| 条目 | 为什么没做 |
|---|---|
| #12 分类预算（给某个分类单独设上限） | 要动 category 的数据结构，属于「加功能」而不是「修问题」，留到下一轮 —— **已由 v1.2 完成** |
| #15 周期性记账（房租 / 订阅每月自动生成） | 同上，需要设计「当月是否已生成」的状态，值得单独做一版 |
| #16 移除 `INTERNET` 权限 | 代码里确实一行网络请求都没有，但去掉它要真机逐项验证分享面板和文件导出，风险大于收益。已在 README 里写明「不会发起任何网络请求」 |

---

## 打包 / 推送工具链的修复（2026-09-18）

出这一版安装包时，在工具脚本里挖出几个真 bug，都已修好：

| 脚本 | 问题 | 症状 | 改法 |
|---|---|---|---|
| `tools/build-apk.mjs` | 传给 `cmd.exe` 的命令里，路径被 Node 转义成 `\"C:\...\gradlew.bat\"`，而 cmd 不认反斜杠转义 | `npm run apk` 在这台机器上跑不起来，报「不是内部或外部命令」 | 改用 `spawnSync('cmd.exe', [...], { shell: true })`，引号交给 cmd 自己处理 |
| `tools/push-to-github.mjs` | **浅克隆下会断历史** —— 本地最早那个提交的父提交没被拉下来。注意 `git log --format=%P` 是**浅克隆感知**的，对边界提交返回空，但它的提交对象里其实写着 `parent` | 推上去后 `main` 只剩 2 个提交，v1.0 和「添加推送脚本」这两段历史**和分支脱开了**（对象还在库里，只是不在历史线上） | 新增 `rawParents()` 从 `cat-file` 的原始对象里读父提交；再用 `resolveParents()` 确认它在远程存在后**原样沿用它的哈希** |
| `tools/push-to-github.mjs` | 本机代理替换了 TLS 证书，Node 内置的 CA 认不出 `api.github.com`，报 `UNABLE_TO_VERIFY_LEAF_SIGNATURE` | `npm run push` 在这个网络环境下直接失败（同样环境下 `gh` 却正常） | 脚本开头加证书探测，需要就自动带 `--use-system-ca` 把**自己重跑一遍**，不用记额外命令 |
| `tools/push-to-github.mjs` | 本机 `git update-ref` **退出码 0 却不生效**：引用只在 `.git/packed-refs` 里，松散引用文件没被建出来，回读还是旧值 | 推完之后 `git status` 一直显示「ahead 2」，看着像没推成功 | 写完**回读校验**，值对不上就直接写 `.git/refs/...` 松散引用文件兜底 |

另外把「内容 tree 哈希不一致」改成**致命错误、当场中止，且不再改动分支指针** —— 原来的逻辑是内容对不上也照样先推分支、推完才报错，等于把远程改成半吊子状态。

> **浅克隆要留意**：这个仓库是 `git clone --depth` 下来的，本地只有最近的提交。
> 想让本地和远程完全一致，先 `git fetch --unshallow` 把历史补全。

---

## 版本号在哪几处

升版本时这几处都要改（**容易漏**，尤其 Service Worker 那个）：

| 文件 | 字段 |
|---|---|
| `package.json` | `version` |
| `android/app/build.gradle` | `versionCode`（每次都要 +1）、`versionName` |
| `ios/App/App.xcodeproj/project.pbxproj` | `MARKETING_VERSION`、`CURRENT_PROJECT_VERSION`（Debug / Release 各一处） |
| `public/sw.js` | `CACHE` 常量 |
| `README.md` | 顶部的「当前版本」 |

> `sw.js` 的缓存名**必须跟着升**。Service Worker 的 `activate` 是靠「缓存名不一样」来清旧缓存的，
> 不升的话老 PWA 用户拿到的还是旧资源。

---

## 关于签名

`android/jizhang.keystore` 和 `android/keystore.properties` **不在版本库里**（`.gitignore` 明确排除），
也**没有随仓库传过来**。要出可安装的 APK 就必须有一把钥匙，所以 v1.1 是**新生成**的一把，
和原作者签 v1.0 用的那把不是同一把。

后果：

- 手机上**没有**装过 v1.0 → 直接装 v1.1，没问题。
- 手机上**已经装了** v1.0（原作者那把签的）→ 装 v1.1 会报签名冲突，必须先卸载。
  **卸载会清掉 App 数据**，所以先「设置 → 导出完整备份」，卸完重装再恢复。

以后每次发新版，**继续用同一把钥匙**，就不会再有这个问题。
`android/jizhang.keystore` 和 `android/keystore.properties` 请另存到网盘或 U 盘 —— 丢了就只能再换一次钥匙。

---

## 下一轮可以做的（候选，按性价比排）

> v1.1 那份清单里的 **#1 分类预算已在 v1.2 完成**（含记账页圆点、设置页管理、备份兼容）。
> 下面是刷新过的清单。

### 功能

1. **周期性记账** —— 记录上加「每月重复」开关，启动时检查当月是否已生成。房租、订阅场景。
   要先想清楚「当月已生成」这个状态记在哪：挂在记录上（`recurringId` + `generatedMonth`）
   比塞进 `meta` 稳，删了记录也不会留下孤儿状态。
2. **账单页按金额筛选** —— 现在只能按分类和关键字筛。「找出所有 > 500 的支出」是很自然的需求，
   改动最小、纯前端筛选逻辑，适合当下一轮的第一件事。
3. **分类预算的「还剩多少」** —— 现在圆点只说状态，不说剩多少。可以在分类格上再压一行小字
   （`剩 61.50`），但格子会变高；先量一下 360px 屏上 5 列塞不塞得下再动手。
4. **导出 Excel（.xlsx）** —— 现在只有 CSV。分月分表、带格式的话 Excel 更实用。
5. **记账页记住上次用的分类** —— 连记同类开销时能少点一下。存在 `meta` 里即可。

### 工程质量

6. **视觉回归测试** —— 现在的 `tools/verify.mjs` 有 68 项断言，但**只核对数字和文案**，
   之前那些排版问题（标题偏 26px、图表字号缩成 9px）它一条都发现不了。
   建议加一层：固定视口（360×800）截图与基线做像素比对，超阈值就报错。
7. **补图表的无障碍标签** —— 圆环图、柱状图还没有可读的文字替代，屏幕阅读器读不出来。
   分类格的预算圆点这次已经补了 `aria-label`，可以照着同一套做法。
8. **`verify.mjs` 里剩下的 `page.click()` 换成 JS 触发** —— 这次踩到了坐标点击打到固定底栏的坑
   （点「今天」实际点到了「账单」标签），只改了那一处。其余按坐标点的位置同样有风险，
   尤其是记账页变长之后。
9. **移除 `INTERNET` 权限** —— 需要真机验证导出 CSV、分享面板、备份恢复三处，验证通过再移除。
10. **重拍 README 的 5 张展示图** —— `docs/screenshots/` 里那几张还是 v1.1 之前拍的，
    记账页那张看不到分类圆点。顺手可以做成 `tools/` 下的一个脚本，让它可复现，
    而不是靠手工截。

### 兼容性

11. **iPad / 平板布局** —— 现在只有手机宽度下好看，横屏或 iPad 上卡片会拉得很宽。
12. **深色模式下的图表配色** —— 可以再针对深色背景微调一档对比度。

---

## v1.0 — 2026-09-17

最初的版本。纯本地安卓记账 App：四个页面（记账 / 账单 / 统计 / 设置），
多账本、分类管理、预算、CSV 导出、完整备份与恢复，数据全部存在本机 IndexedDB。

- 技术栈：原生 HTML/CSS/JS（无框架）+ Vite 5 + Capacitor 6
- 图表：手写 SVG，不依赖图表库
- 金额一律以「分」存整数，避免浮点误差
