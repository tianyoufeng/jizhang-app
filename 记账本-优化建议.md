# 记账本 v1.0 — 优化建议清单

> 基线：`tianyoufeng/jizhang-app` 仓库 3 个提交（2026-09-17 建库）。
> 全部结论来自通读 `index.html`、`src/**`、`android/**`、`tools/verify.mjs`，非泛泛而谈。
> 定位前提：**纯本地自用，不引入后端、不引入框架、不引入图表库**——以下建议都守住这条线。

---

## P0 · 改几行 CSS / JS 就见效

### 1. 顶栏标题不居中
- **位置**：`src/styles.css:63-83`
- **问题**：`#topbar` 是 `display:flex`，左边「账本切换」按钮最宽 132px，右边主题按钮固定 40px，中间的 `h1 { flex:1; text-align:center }` 只在**剩余空间**里居中。账本名叫「日常账本」时按钮约 92px，标题实际偏右 ≈26px。
- **改法**：左右做成等宽列，标题自然回到屏幕正中。
  ```css
  .ledger-btn { flex: 0 0 92px; justify-content: flex-start; }
  #topbar .icon-btn { flex: 0 0 92px; justify-content: flex-end; }
  #topbar { gap: 0; }
  ```

### 2. 底部导航图标用系统 emoji，不可控
- **位置**：`index.html:27-30`、`src/styles.css:131-147`
- **问题**：
  - `✏️📋📊⚙️` 是 emoji，小米 / 华为 / 三星 / 原生安卓各渲染一套，视觉完全不可控；
  - 未选中态靠 `filter: grayscale(1) opacity(.55)`，但在不同 ROM 的 emoji 字体上效果不一致，有些仍偏彩色；
  - emoji **无法跟随 `--primary`**，所以选中的 tab 只有文字变蓝、图标还是原色，选中态是割裂的。
- **改法**：换成内联 SVG，和顶栏那个 chevron 一样用 `stroke="currentColor"`，这样颜色自动跟主题走。项目里已有手写 SVG 的先例（`charts.js`），改造成本很低。建议图标：编辑 / 列表 / 柱状图 / 滑块。

### 3. 两个图表的字号一个被放大、一个被缩小
- **位置**：`src/charts.js:9-50`（饼图）、`src/charts.js:74-119`（趋势图）、`src/styles.css:388-389`
- **问题**：`.chart-wrap svg { width:100%; height:auto }` 会覆盖 svg 的固有尺寸，文字按 `viewBox` 比例一起缩放：

  | 图 | viewBox 宽 | 360px 屏实际渲染宽 | 缩放比 | 设计了 10px 的字实际是 |
  |---|---|---|---|---|
  | 趋势图 | 340 | ≈304 | 0.89 | **≈9px** |
  | 饼图 | 200 | ≈304 | 1.52 | 12px → **≈18px** |

  趋势图的月份标签、`最高 8000.00` 在小屏（320px）会掉到 ≈8px 以下；饼图的图内文字反而比旁边的图例（CSS 13px）大一截，同一张卡片里字阶是乱的。
- **改法**：所有图统一 `viewBox` 宽度基准（比如都按 320），并把图内字号按 11–12px 设计；或者给 `.chart-wrap` 设 `max-width` 让缩放比稳定在 1.0 附近。修完记得在 `tools/verify.mjs` 里加一条固定视口的断言。

### 4. 趋势图图例被 `flex:1` 撑开
- **位置**：`src/charts.js:112-117` + `src/styles.css:391-395`
- **问题**：`.legend-name { flex:1 }`，而趋势图的图例是「点·收入·点·支出」四个元素塞在**同一个** `.legend-row` 里。第一个 `.legend-name`（收入）吃掉所有剩余空间，把「支出」推到最右边，看起来像两个互不相关的信息。
- **改法**：拆成两个独立分组，外层用 `display:flex; gap:16px`：
  ```html
  <div class="legend-row" style="gap:16px">
    <span style="display:flex;align-items:center;gap:6px"><i class="legend-dot" style="background:var(--income)"></i>收入</span>
    <span style="display:flex;align-items:center;gap:6px"><i class="legend-dot" style="background:var(--expense)"></i>支出</span>
  </div>
  ```

### 5. 金额输入允许多个小数点，报错文案指错了问题
- **位置**：`src/views/txForm.js:123-127`（输入过滤）、`src/views/txForm.js:108-112`（报错文案）、`src/utils.js:25-32`（校验正则）
- **问题**：输入过滤只做 `replace(/[^\d.]/g, '')`，**允许出现第二个小数点**。用户输入 `1.2.3` 时，`yuanToFen` 的正则 `^\d*(\.\d{0,2})?$` 判为非法，界面弹出「金额只能填数字，最多两位小数」——可用户确实只输了数字和点，会完全懵。
- **改法**：输入时就只保留一个小数点、最多两位小数：
  ```js
  draft.amount = amountEl.value
    .replace(/[^\d.]/g, '')
    .replace(/^(\d*\.\d{0,2}).*$/, '$1');   // 截断多余小数位
  ```
  或者把报错文案改成「只能有一个小数点，最多两位小数」。

### 6. 统计数字缺等宽对齐
- **位置**：`src/styles.css:323`（`.summary .s-val`）
- **问题**：`.tx-amt`、`.legend-amt`、`.d-sub` 都加了 `font-variant-numeric: tabular-nums`，唯独 `.s-val`（收入/支出/结余那三个大数字）没加。翻月份时数字宽度变化会让三列轻微抖动。
- **改法**：补上 `font-variant-numeric: tabular-nums;`。

### 7. 三套间距各说各话
- **位置**：`src/styles.css:150-156`（`.card` 12px）、`:343`（`.day-group` 14px）、`:413`（`.set-group` 18px）
- **问题**：12 / 14 / 18 三套垂直节奏混用，页面之间松紧不一致。
- **改法**：定一套 4 的倍数 scale（如 8 / 12 / 16 / 24），用 CSS 变量统一：
  ```css
  --gap-1: 8px; --gap-2: 12px; --gap-3: 16px; --gap-4: 24px;
  ```

### 8. 次级字号整体偏小
- **位置**：`src/styles.css:141`（`.tab` 11px）、`:324`（`.s-key` 11px）、`:233`（`.cat-item` 11px）、`:395`（`.legend-pct` 11px）
- **问题**：手机端 11px 已经接近可读下限，配合 15px 的正文会让整个界面显得「字挤在一起」。
- **改法**：11px 整体提到 12px，`.tab-txt` 可以到 12px 并适当加 `letter-spacing`。

---

## P1 · 功能上真正缺的东西

### 9. 搜索只覆盖当前月份
- **位置**：`src/store.js:232-243`
- **问题**：`monthTransactions()` 先按 `monthOf(t.date) === month` 过滤，再跑关键字匹配。所以想找「三个月前那笔买空调的钱」，得逐月翻着搜。
- **改法**：账单页加一个「本月 / 全部时间」切换。关键字段上已经建了 `ledger_date` 索引，跨月查询不会变慢。

### 10. 分类不能排序（`order` 字段是死的）
- **位置**：`src/store.js:63-69`（初始化写 `order: i`）、`src/views/settings.js:287-340`（分类管理）
- **问题**：新建分类时按 `order` 排，但**没有任何 UI 能改 `order`**。常用的「餐饮」永远在第一格没法调到顺手的位置，用户只能在脑子里记。
- **改法**：分类管理里加长按拖拽，或者更省事的方案——每个分类行右侧加「置顶」按钮，把 `order` 改成负数即可。

### 11. 删除无法撤销
- **位置**：`src/store.js:224-227`（`deleteTransaction` 直接 `db.remove`）
- **问题**：确认框点完就是硬删除，没有后悔药。`toast()` 已经支持自定义时长（`ui.js:5`），加撤销成本很低。
- **改法**：删除后不立即落库，先在 toast 里挂一个「撤销」，5 秒后再真正 `db.remove()`。

### 12. 只有账本级预算，没有分类预算
- **位置**：`src/store.js:117`（`addLedger(name, budgetFen)`）、`src/views/record.js:23-41`
- **问题**：现在只能给整个账本设一个「每月支出上限」。但真正的痛点往往是**某个分类**失控——「这个月餐饮花了 3000」比「总支出超了 500」更有指导意义。
- **改法**：给 category 加一个可选的 `budgetFen` 字段，记账页的分类格右上角显示一个小圆点（超支时变红）。

### 13. 统计页缺「环比」
- **位置**：`src/views/stats.js:22-51`
- **问题**：有单月总结、有 6 个月趋势，但**没有「这个月比上个月多花了多少」**——而这恰恰是记账用户最想知道的那句话。
- **改法**：在「X 月总结」卡片里加一行环比：`比上月多花 412.30 元（+18%）`，涨红跌绿，用现成的 `monthSummary()` 算上个月即可。

### 14. 没有备份提醒
- **位置**：`src/views/settings.js:96-104`（关于分组）
- **问题**：README 反复强调「建议每隔一两个月导出一次备份」，但 App 自己完全不提。而 `allowBackup="false"` + 数据只在本机 = 手机一丢全没了。
- **改法**：`exportPayload` 的时候顺手把时间写进 `meta.lastBackupAt`，设置页显示「上次备份：32 天前」，超过 30 天在「记账」页顶部给一条黄条提示。

### 15. 缺周期性记账
- **位置**：`src/views/list.js:200-212`（已有 `copyToToday`）
- **问题**：房租、房贷、各类订阅每月重复，现在得手动点「再记一笔一样的」。作者已经做出了复制的雏形，再往前一步就是周期化。
- **改法**：记录上加一个「每月重复」开关，App 启动时检查当月是否已生成。

---

## P2 · 兼容性与工程质量

### 16. `INTERNET` 权限和「不联网」的定位自相矛盾
- **位置**：`android/app/src/main/AndroidManifest.xml:41`
- **问题**：Capacitor 模板默认带 `<uses-permission android:name="android.permission.INTERNET" />`。代码里确实**没有任何网络请求**，但权限声明在，安装时系统也会列出来——这和 README 主打的「不联网」在观感上是打架的。
- **改法**：Capacitor 的本地内容是靠 `WebViewLocalServer` 拦截 `https://localhost` 直接返回本机文件，理论上不需要联网权限。**值得一试，但必须真机验证**（重点验导出 CSV、分享面板、备份恢复）。如果移除后有问题就退回来，同时在 README 里补一句「代码零网络请求」并加一条 CSP 说明。

### 17. 声明支持 Android 5.1，但部分 emoji 在旧字体上是豆腐块
- **位置**：`android/variables.gradle:2`（`minSdkVersion = 22`）、`README.md`（「需要安卓 5.1 以上」）、`src/views/settings.js:11-15`（`EMOJI_CHOICES`）
- **问题**：Android 5.1 的 emoji 字体只覆盖到 Unicode 7.0 左右。以下 emoji 都是之后才加入的，在旧机上大概率显示成方框：
  - 🛒（Unicode 9.0，**这是默认分类「购物」的图标**）
  - 🛵、🧻（9.0+/11.0）、🩺（12.0）、🪙（13.0）
- **改法**：两条路选一条——要么把 `minSdkVersion` 提到 23+ 并同步改 README（现实里几乎没人还在用 5.1），要么把上面几个换成年份更早的 emoji。

### 18. 一批没人调用的死代码
| 位置 | 说明 |
|---|---|
| `src/db.js:24` | `createIndex('ledger_date', ...)` 建了复合索引，但 `store.js` 所有查询都走内存 `filter`，**索引从未被使用** |
| `src/db.js:62` | `db.get()` 无调用方 |
| `src/db.js:102` | `db.clear()` 无调用方（实际用的是 `clearAll()`） |
| `src/ui.js:149` | `pickOption()` 无调用方 |
| `src/ui.js:187` | `field()` 无调用方 |
| `src/ui.js:47` | `handle.setTitle()` 无调用方 |
| `src/ui.js:72` | `openSheet({ onMount })` 无调用方 |
- **说明**：不影响运行，但会误导后来者（比如让人以为查询走了索引）。建议删掉，或者留着但在注释里标注「预留」。

### 19. 分类名 6 个字在 5 列网格里会被截断
- **位置**：`src/styles.css:217-238` + `src/views/settings.js:352`（`maxlength="6"`）
- **问题**：`.cat-grid` 固定 5 列，360px 屏上每格约 63px，减去内边距后文字可用约 59px。6 个中文字（11px）约 66px → 触发 `text-overflow: ellipsis`。用户被允许取 6 个字，却看不全。
- **改法**：网格改成自适应 `grid-template-columns: repeat(auto-fill, minmax(64px, 1fr))`，或者把 `maxlength` 收到 4。

### 20. 切 Tab 会丢滚动位置
- **位置**：`src/main.js:32-33`
- **问题**：`renderApp()` 每次都 `viewEl.scrollTop = 0; window.scrollTo(0,0)`。账单页翻到下半截，切到统计再切回来，位置归零。
- **改法**：用一个 `{ [tab]: scrollTop }` 对象记住每个 tab 的位置，切回来时恢复。

---

## 附 · 关于 `tools/verify.mjs`

那 53 项走查写得很扎实，但它**只断言数字和文案**（`check('账单：支出合计', ..., '50.50')` 这种），上面这些排版问题它一条都发现不了。

建议补一个视觉回归层：固定视口宽度（比如 360×800），把每个页面的截图与基线做像素比对，超出阈值就报错。这样「顶栏标题偏了 26px」「图表标签缩成了 9px」这类问题改一次代码就能立刻被抓到。

---

## 一句话优先级

**先做 1、2、3、4、5**——这五条全是纯前端改动，加起来大概一两个小时，但能解决最扎眼的排版问题和最典型的交互 bug。
**再做 9、11、13、14**——这四条是「用户真的会用到」的功能，且都不需要动数据结构。
**16、17 属于定位一致性问题**，可以根据你对「老安卓兼容」的实际态度决定做不做。
